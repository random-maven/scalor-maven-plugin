<?xml version="1.0" encoding="UTF-8"?>
<lifecycleMappingMetadata>

    <!-- Used only by Maven, not M2E. -->
    <pluginExecutions>

        <!-- Install companion Eclipse plugin. -->
        <pluginExecution>
            <pluginExecutionFilter>
                <goals>
                    <goal>eclipse-config</goal>
                </goals>
            </pluginExecutionFilter>
            <action>
                <execute>
                    <runOnIncremental>false</runOnIncremental>
                    <runOnConfiguration>true</runOnConfiguration>
                </execute>
            </action>
        </pluginExecution>

        <!-- Delegate goals to companion Eclipse plugin. -->
        <pluginExecution>
            <pluginExecutionFilter>
                <goals>

                    <goal>register-macro</goal>
                    <goal>register-main</goal>
                    <goal>register-test</goal>

                    <goal>compile-macro</goal>
                    <goal>prepack-macro</goal>

                    <goal>compile-main</goal>
                    <goal>prepack-main</goal>

                    <goal>compile-test</goal>
                    <goal>prepack-test</goal>

                    <goal>link-scala-js-main</goal>
                    <goal>prepack-linker-main</goal>

                    <goal>link-scala-js-test</goal>
                    <goal>prepack-linker-test</goal>

                </goals>
            </pluginExecutionFilter>
            <action>
                <ignore />
            </action>
        </pluginExecution>

    </pluginExecutions>

</lifecycleMappingMetadata>
