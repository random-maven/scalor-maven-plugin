
//def masterXml = new File(basedir, "resources/source/master-1/pom.xml")
//def masterText = masterXml.text
//masterText = masterText.replace("@project.groupId@", projectGroupId)
//masterText = masterText.replace("@project.artifactId@", projectArtifactId)
//masterText = masterText.replace("@project.version@", projectVersion)
//masterXml.write(masterText)

//def settingsXml = new File(basedir, "home/.m2/settings.xml") 
//def settingsText = settingsXml.text
//settingsText = settingsText.replace("@project.localRepo@", projectLocalRepo)
//settingsXml.write(settingsText)

assert true
